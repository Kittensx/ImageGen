# IMAGE_GEN common Python dependencies.
#
# GPU-specific packages are intentionally not listed here. Run install.bat so
# IMAGE_GEN can select a validated PyTorch/CUDA/custom-attention profile for the
# detected NVIDIA GPU and driver.

safetensors
spandrel==0.4.2
diffusers
transformers
accelerate
matplotlib
lark
kornia
open-clip-torch
einops
tqdm
PyYAML>=6
fastapi>=0.115,<1
python-multipart>=0.0.9,<1
uvicorn[standard]>=0.30,<1
pytest
psutil>=5.9,<8
peft
